# AFFiNE 환경 변수 샘플 (.env 로 복사해서 값을 채우세요)

# PostgreSQL 비밀번호. postgres 서비스와 DATABASE_URL 이 같이 사용한다
# DATABASE_URL 안에 그대로 들어가므로 @ : / ? # 같은 특수문자는 쓰지 않는다
POSTGRES_PASSWORD=CHANGE_ME_POSTGRES_PASSWORD

# SMTP 로그인 비밀번호 또는 API 키. 메일 발송을 안 쓰면 그대로 둬도 된다
MAILER_PASSWORD=CHANGE_ME_SMTP_PASSWORD
