# SnapOtter 환경 변수 샘플 (.env 로 복사해서 값을 채우세요)

# PostgreSQL 사용자(snapotter) 비밀번호. snapotter 와 postgres 가 같은 값을 사용
# DATABASE_URL 안에 그대로 들어가므로 @ : / ? # 같은 특수문자는 쓰지 않는다
POSTGRES_PASSWORD=CHANGE_ME_POSTGRES_PASSWORD
