# Authentik 세션/토큰 암호화용 시크릿 키 (openssl rand -base64 60 으로 생성)
AUTHENTIK_SECRET_KEY=CHANGE_ME_AUTHENTIK_SECRET_KEY

# PostgreSQL authentik 사용자 비밀번호 (DB와 Authentik이 같은 값을 사용)
PG_PASS=CHANGE_ME_POSTGRES_PASSWORD
